<% @tasks.each do |task| %>
    <%= link_to '[x]',
        task_path(task),
        method: :delete,
        data: { confirm: 'Sure?' } %>
    <br>
    <%= link_to task.title, task_path(task) %>
    <br>
    <%= task.body %> 
    <br>
    <br>
<% end %>
